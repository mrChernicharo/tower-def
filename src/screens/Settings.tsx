import { Link } from "react-router-dom";

const Settings = () => {
    return (
        <>
            <Link to="/">←</Link>
            <p>Settings</p>
        </>
    );
};

export default Settings;
