import { Link } from "react-router-dom";

const Achievements = () => {
    return (
        <>
            <Link to="/">←</Link>

            <p>Achievements</p>
        </>
    );
};

export default Achievements;
