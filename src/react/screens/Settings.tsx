import { Link } from "react-router-dom";

const Settings = () => {
    return (
        <>
            <Link to="/area-selection">←</Link>
            <p>Settings</p>
        </>
    );
};

export default Settings;
